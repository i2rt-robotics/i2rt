# LeRobot recorder — workstation env (SEPARATE from the YAM/i2rt env).
#
# rclpy is NOT pip-installed: it comes from your ROS 2 install
#   source /opt/ros/<distro>/setup.bash
# Use the SAME ROS 2 distro as the robot machine so DDS discovery matches, and
# set a matching ROS_DOMAIN_ID on both machines.
#
# Suggested:
#   python -m venv ~/lerobot_env --system-site-packages   # see ROS rclpy
#   source ~/lerobot_env/bin/activate && source /opt/ros/<distro>/setup.bash
#   pip install -r requirements.txt

lerobot
pyrealsense2
PyQt5
numpy
